Issues:

* Allow different permissions for directories
* Document the exact logic of syncs
* Swap params (file and action)
* Clarify prompt on conflict resolution. When I choose "source", does it mean that "source" is overwritten or "source" is used.

